# Kanäle (`channels` area)

Endnutzer-Sicht: siehe [guide-web-admin.md#kanäle](guide-web-admin.md#kanäle).

The server's channels as Discord's sidebar (issue #259): `pages/ChannelsPage.tsx` is **one list** (`components/channels/ChannelTree.tsx`: categories fold, one line per channel with check box, type, name large in mono, status only as badge *Event* / *vergangen*) plus a side panel with three figures (Kanäle, vergangene Events — a click selects them —, im Archiv). Everything else about a channel — event, topic, purposes, slowmode, rights — is its **tooltip** (`channelTip()` in `lib/channels.ts`); the purposes table (what the bot uses a channel for, still settings) opens as a dialog (`PurposeList.tsx`). Don't put it back on the page.

- **Threads nest under their channel, three levels deep (#361).** Discord gives a thread's `parentId` the id of the *text channel* it hangs off, not a category — `discord.listAllChannels()` marks it (`isThread`, `THREAD_TYPES` = announcement/public/private thread) so `groupByCategory()` (`lib/channels.ts`) can resolve its real category through that channel instead of dropping it into "Ohne Kategorie": each `ChannelNode` in a group carries its own `threads: Channel[]`, and only a thread whose parent channel is not in the list at all (filtered out, deleted) still falls back to a loose row of its own. `ChannelTree.tsx` renders a thread's row right below its channel, indented (`kn-row-thread`) and folded per channel (`kn-fold-sm`, open by default, always open while searching); selecting a whole category selects its channels' threads too. A thread only offers **rename and delete** — moving it into another category or duplicating it does not apply, so *Bearbeiten* and *Duplizieren* stay hidden on its row.

- **Inline rename**: double click or pencil, Enter saves, Esc/blur discards. Discord's name rules apply while typing — `src/utils/channelNames.js` is the tested source, `lib/channelNames.ts` its twin (regex and body held identical by `test/utils/channelNames.test.js`; `schemaName()` in `lib/eventPlan.ts` inlines the same regex). **The rules are Discord's own (#285):** lower case, whitespace → `-`, doubled dashes collapsed, and only ASCII punctuation except `-`/`_` (plus control characters) is removed — emojis and Unicode symbols (`・ │ ┃ 【】 ⚔`) **stay**, so `🔥・mi-16-09-ssc-tk` keeps its design; `.` is stripped, so `16.09` is stored as `1609`. Don't go back to a letters-digits-only whitelist.
- **New event channels look like the previous ones (#285, `src/web/channelNaming.js`).** One answer for `/event anlegen` (new and duplicate), the web's create dialog, quick-create, *Umbenennen nach Schema* with an empty schema and `/kanal anlegen` without a name: `deriveChannelName({ guildId, categoryId, date, instanceIds | raid, fromEventId })` → `{ name, source: "previous" | "schema" | "default", label, detail, design, fromChannel, replaced: [{ part, from, to }], templateChannelId, placement }`; the pure halves are `namingContext()` / `nameFor()` / `describeResult()` (events from `loadEventGroups` with the lookback window, live channels, `channelArchiveStore` schemas — every read best-effort). Order: a **stored own schema** of the category wins (the default schema stored by quick-create's "merken" does *not* count as one); else the **latest event channel of the category** (the one with the same raid tag first; for a duplicate the chosen event's channel), taken apart by `derivePatternFromName()` in `utils/channelNames.js` — **only tokens equal to that event's own Berlin day** are replaced (`dd-mm`, `dd_mm`, `ddmm`, `dd-mm-yy(yy)`, `yyyy-mm-dd`, `d-m`, weekday de/en short/long) and the raid only where the event's instance tag (`ssc-tk`, `ssctk`, `ssc_tk`, ids, the stored raid) stands as a whole word; a name without a recognisable date falls to the **default schema with the channel's symbol prefix** (`prefixOf`). Anything else in the name — `t6`, `25er`, emojis, separators, order — is never touched, and a missing new raid keeps the old one. **Design:** the new channel is a *copy* (`discordChannels.createFromTemplate`) of the stored template channel, else of that latest event channel (a duplicate always copies its source); only a category without any event channel gets a plain channel with the category's rights. **Position:** `placementFor(eventChannels, day)` → right after the channel of the latest date ≤ the new day, else before the next one; `discordChannels.placeChannel()` / `positionTarget()` do the `setPosition` (best-effort, never fails the create; a series sorts each new channel behind the one it just made). **Visible:** the bot's step 1 says `neu wie #… — Wochentag, Datum und Raid werden ersetzt` and the confirmation `Name: \`…\` · abgeleitet aus #… (Datum 16-09 → 23-09)` plus the design line; the web shows one `components/channels/NamingBadge.tsx` (label on the badge, `detail · design` in its tooltip; `short` per row in the bulk rename) — under the name field of `RaidCreateDialog` (suggestion from `GET /api/raids/channel-name`, area `raids`; a hand-typed name reads "von Hand benannt"), above the quick-create preview (`naming` in the batch answer; empty schema = derived, `templateChannelId` empty = copy of the derived channel) and per row in the rename preview (empty schema = derived; with nothing to derive from, the name stays). `createEvent` answers with `channelNaming`. Tests: `test/utils/channelNameDerivation.test.js`, `test/web/channelNaming.test.js`, `test/web-client/channelNaming.test.js`.
- **A category's naming schema has its own place**: the pencil on the category head (`ChannelTree.tsx`, hover like the row icons, only `channels` write) opens `components/channels/CategorySchemaDialog.tsx` — schema with the placeholder chips, raid and template channel folded under *Raid & Vorlage*, and the next three weekly names as preview (quick-create's dry run with `ignoreStoredSchema: true`, so an empty field previews the *derived* names, not the schema stored so far). Saving goes to `POST /api/channels/schema` (`saveSchema` in `apiRoutes/channels.js`: category of this guild, ≤ 200 characters, must render to a name, template channel of this guild) → `channelArchiveStore.saveCategorySchema`; an empty schema means "wie der letzte Event-Kanal" again, the remembered event time stays. A category with an own schema carries a small *Schema* badge (schema in its tooltip; `ownSchemaOf()` in `lib/channels.ts` — the default schema an old quick-create stored does not count). Quick-create's "merken" still stores too.
- **`{mon}` — the month in three English letters** (`jan feb mar apr may jun jul aug sep oct nov dec`, `MONTHS` in `utils/channelNames.js`; `schemaName()` in `lib/eventPlan.ts` has the same list). Deriving recognises it too: `dd-mon`, `d-mon`, `mon-dd`, `dd-mon-yy(yy)`, with `-`, `_` or no separator. A date's edges are checked by kind (`dateEdge`): digits must not run on into digits, the month's letters not into letters — `16-sept` is no date.
- **Selection → bulk bar** (`ChannelBulk.tsx`): *Kategorie … / Thema …* open one dialog where every field starts at "unverändert" and only changed fields are sent (`bulkChanges()`); *Umbenennen nach Schema …* previews on the server (`POST /api/channels/rename-preview`, date from the channel's event). Changes go **one channel per request with a pause** (`runInSteps()`, progress in the job toast) because Discord bounces bursts; the server's `channelOps.runSerial()` pauses between channels too and answers per channel (`"3 Kanäle geändert, 1 fehlgeschlagen: fehlende Rechte"`).
- **Archive, never auto-delete**: *Archivieren* moves into the archive category and denies writing to every overwrite plus @everyone (the bot keeps its own); `channelArchiveStore.js` (`data/settings/channel-archive.json`) logs who/when and holds the archive category per server, the hint deadline (`archiveDeleteHintDays`, default 14) and the quick-create schema per category — kept out of the general config so `channels` write is enough. **Deleting** always needs the name typed (several: `LÖSCHEN`, checked again on the server). From the **Archiv tab** `discordChannels.deleteChannel()` refuses any channel outside the archive category, whatever the caller sent. From the **channel list** (trash icon per row, *Löschen …* in the bulk bar beside *Archivieren*) the request carries `anywhere: true` — any channel may go, also without an archive category, but never a category (`fetchChannel` hands none out); the dialog first names every channel of an **upcoming** event whose signup message goes with it (`deleteWarnings()` in `lib/channels.ts`). Archiving stays the suggested way for anything that may be needed again. The waiting count shows on the page and as the dashboard task `channels` (`buildTasks`, yellow past the deadline, only for users who can read `channels`).
- **Quick-create** (`QuickCreateDialog.tsx`, `POST /api/channels/batch`): schema empty (= like the previous event channel, see above) or typed (`{tag}-{dd}-{mm}-{raid}`, placeholders in `PLACEHOLDERS`), once or weekly × n, optional template channel (clone: rights, topic, slowmode; empty = copy of the derived channel); `dryRun` answers the plan with `exists`, existing names are skipped, never duplicated. Drag & drop between categories is deliberately not built.
- **"Gleich Event anlegen"** (quick-create, only with a category and for `raids` write — `canCreateEvents` in `GET /api/channels`, checked again on the server): one switch and one time field in the dialog, the category's default raid template and source as one small badge with the details in its tooltip (`eventDefaults` per category: `categoryRaidTemplate` + `categorySignupSource`). `POST /api/channels/batch` with `withEvent: true, time` creates each channel and then an event in it through `eventCreate.createEvent()` (date from the series, title = template name, else category name, `raidTemplateId` of the category default, `signupSource` of the category). A failed event leaves its channel; the result carries `eventId` / `eventError` per channel and `message` lists every channel whose event failed, one per line (`.toast-msg` keeps line breaks). The time is remembered with the schema (`schemas[cat].time`).
- Discord writes live in `src/web/discordChannels.js` (edit/archive/delete/createCategory/createFromTemplate), not in `discord.js`; the upcoming-event badges come from `raidEventGroups.fetchEventsCached()` bounded to 4 s, past ones from `raidEventStore`.
