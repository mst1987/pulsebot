.env
.env.dev
node_modules
logs/
data/
reference/cla/appsscript/
reference/rpb/appsscript/
service-account.json

# The wowsims binary is fetched, not committed (scripts/fetch-wowsimcli.js).
bin/

# Per-user Claude Code overrides (permissions etc.) stay local.
.claude/settings.local.json

# Every env variant is local only (.env.dev, .env.dev.bak, ...); the example is checked in.
.env.*
!.env.example

# Coverage reports, backups, logs and Claude Code's scheduler lock.
coverage/
*.bak
*.log
.claude/scheduled_tasks.lock
